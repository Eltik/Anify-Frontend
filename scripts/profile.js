async function load(username) {
    /*
    const login = await getLogin();
    let data = null;

    if (!login || login.error) {
        const req = await fetch(api_server + "/user", { method: "POST", body: JSON.stringify({ username: username }), headers: { "Content-Type": "application/json" } })
        const res = await req.json()
        data = res.data.User;
    } else {
        data = login.data.Viewer;
    }

    if (!data) {
        return;
    }
    */
}